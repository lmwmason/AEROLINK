[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aerolink-pi"
version = "0.1.0"
requires-python = ">=3.11"
description = "AEROLINK per-vehicle companion software"

[tool.setuptools.packages.find]
where = ["src"]
